# Project Agent MCP — example environment

# Network
HOST=127.0.0.1
PORT=7777
TIMEZONE=America/Edmonton

# Vault (Obsidian) root directory
# Set this to your local vault path (absolute)
VAULT_ROOT=/Users/occam/Documents/obsidian

# Top-level folders to scan and resolve documents across (comma-separated)
# Default is "Projects" if unset
# If any values include spaces, wrap in quotes
PROJECT_ROOTS="Projects,Notes,Project Research"

# Auth
# Bearer token for development/testing (optional)
DEV_BEARER_TOKEN=
# Comma-separated allowlist of emails (used by proxies that inject x-user-email)
EMAIL_ALLOWLIST=you@example.com
# Force an email identity (dev only); normally provided via header or connector
EMAIL_OVERRIDE=

# Read-only guard (blocks non-GET methods when true)
READONLY=false

# Git auto-push after write commits (best-effort)
GIT_AUTO_PUSH=true
GIT_REMOTE_NAME=origin

# Rate limits
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=1 minute

# Payload/ops limits
MAX_OPS_PER_CALL=128
MAX_LINE_LENGTH=16384
SNAPSHOT_MAX_BYTES=262144
SNAPSHOT_TAIL=10

# TLS (dev) — optional; mTLS supported if TLS_CA_PATH is provided
TLS_CERT_PATH=certs/dev-cert.pem
TLS_KEY_PATH=certs/dev-key.pem
TLS_CA_PATH=
MTLS_REQUIRED=false
